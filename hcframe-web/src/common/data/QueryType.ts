enum sign {
  EQUAL = 1,
  LIKE = 2,
  IN = 3,
  BETWEEN = 4,
  LT = 5,
  GT = 6,
  LTE = 7,
  GTE = 8
}

enum logic {
  AND = 1,
  OR = 2
}

enum curves {
  L_CURVES = 1,
  R_CURVES = 2
}

export { sign, logic, curves }
